import { SheetViewCanvasDirective } from './sheet-view-canvas.directive';

describe('SheetViewCanvasDirective', () => {
  it('should create an instance', () => {
    const directive = new SheetViewCanvasDirective();
    expect(directive).toBeTruthy();
  });
});
